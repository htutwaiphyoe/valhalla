Folder structure

config => configuration files
controller => api routes handlers
components => react components
models => model for database
pages => web pages
pages/api => api routes
public => static files

next-connect package is a routing and middleware for nextjs and make api routes handlers easily like ExpressJS.

postman is used to test API. First create environment for project and then create collection for project and save API enpoints
in there.

mongoose is used for database. create dbConnect file in config for database connection. add environment variables in next.config.js.
In express, connection is in app.js but in nextjs, each api has to call connection method (dbConnect)

In nextjs, mongoose model needs to return existing model if it is already created. mongoose.models.ModelName || mongoose.model("Model", schema) is needed
in export default. If the condition is not included, it will return error.

seeder.js is created to add all rooms into database. To run that file, need npm script in package.json with the command
"node utils/seeder". We have to use node command to run and if we use node command, we have to change import to require.

we can retrieve json data from .json by importing it

get query params in url with req.query. handle room not found error for invalid id with right format

findByIdAndUpdate gives null with invalid id with right format.

findByIdAndDelete also gives null with invalid id with right format and give deleted data if success.

create custom error class in utils folder, status means code in next.

like express, next-connect provides global error middleware which is a error-first function with 4 paramenters. If any middleware throws an error
it will execute. To throw an error, use throw or next() function. To catch all errors, give that middleware in nc({ onError }) option of next-connect

create catch async error middleware and handle validation error and cast error. make sure to show different errors for
production and development mode

for search, use $regex to search keywords in text. $options is for regex options, i means case-insensitive. make sure 
field name is search field. 

for filter, remove unecessary fields 

for paginate, get page, get limit and calculate skip. don't forget to chain database query for each operation.

start frontend, add _document file with bootstrap cdm links

add Navigation, Footer, Layout, _app & Home. css is only in global file. 

add meta tags and title

data pre-fetching with getStaticProps, getStaticPaths & getServerSideProps

create redux store. store needs a wrapper for nextjs to manipulate state (next-redux-wrapper). create actionTypes, reducer and actions for all rooms.
get data with getServerSideProps of wrapper and display all rooms on HomePage 

use react-toastify package for toast message. for toast, toast container and toast emitter are needed. toast container is
defined in layout and toast emitter is defined in the component we want to. position means where toast message appears.

create roomDetails reducer, actionTypes and action. then create RoomDetails page and RoomDetails component. make data pre-fetching with getServerSideProps and render component with actual data. create RoomFeatures component for features and render in RoomDetails component.

to add pagination, react-js-pagination is used. Import and render its component in Home component. pass props to that component. handle click event and push url with page number. dispatch actiion with new page number in HomePage component with getServerSideProps. fetch new data set  and render them. 

add Search Component and search with location, guestCapacity and category. redirect to index and dispatch filter data and fetch data and render them in Home page

add user model and enum object is enum: { values: ['Coffee', 'Tea'], message: '{VALUE} is not supported' }. use validator package for email validation

install mongoose-unique-validator and add this plugin in useSchema

for authentication, authController is created and create signup function for signing up. then add api for signup and test that api. for encryption password, use pre save middleware in user model and bcrypt is used to encrypt it

for login, next-auth package is used. firs create dynamic routes file [...nextauth] in api/auth folder. then create NextAuth and Provider for authentication. Credential is used for custom email, password authenication. it needs authorize function and it has credential argument which includes body data and then implement login functionality in that function and return success promise. for comparing password function, schema method is used.
for session, jwt is used and set jwt: true in session object. 
then add callback functions in callbacks object. jwt function is when jwt is created such as sign in or updated.
add user which is returned from authorize function is added as a property of jwt token and return that token as a success promise.
session callback is called whenever session is checked. add user in token which is returned from jwt callback as a property of session object and return that session as a success promise.

create Login, LoginPage component and send request using signIn method in next-auth/client. it gives result object. if it has error, show error and else redirect to home page. When request is sent, ButtonLoader is shown and button is disable.

for image upload, cloudinary service is used. first install cloudinary npm package. then register account in cloudinary website to get api key. add api key in .env file. add configuration in auth controller and upload avatar to cloud when signup. then store actual avatar data in database

first create Signup component and add two ways binding in input. file input does not need value binding, just onChange. handle file onChange event to get image preview. first create FileReader and if file reading is complete set file to the state and add file.readAsDataURL(e.target.files[0]). choose file data is in e.target.files.
avatar preview value is linked with src in img tag for preview.

handler onSubmit and check input validations. if ok, dispatch signup action. create userReducer and action and actionTypes. if success, redirect to login page

to get current logged in user data, first create get api route /api/me. then create controller for getting data and create middleware to check user is authenticated or not.

create reducer, actions and actionTypes for loading loggedin user in state. 

show navigation item conditionally, use useSession() hooks to check user is authenticated or not

logout with signOut in next-auth/client, default is page reload but to redirect and not reload page, use async version of signOut

create api route for update profile api/me/update, first create controller, in there first check user exists and then replace user data with request body data and be aware this route is only for authenticated user

create reducer, actionTypes and actions for update profile 

create Profile component to update user profile

create schema method for generating reset password token in userSchema

configure email with mailtrap and nodemailer and create forgot password controller, fist get email and fetch user then get token and save user. then create reset url and message and send email. use try/catch for sending mail and if there is an error, delete data related with reset and save.

create ForgotPassword component, reducer, action and page and send email

use SendGrid for real world email sending, get username and password in SMTP relay and configure server and auth user and password. to send real email, sender authentication is needed for sender email

create reset password api route /api/password/reset/:token, first get toke from request query, hash with crypto and fetch user with hashed token and current time, check user exists, then check password match and save user

create ResetPassword component, reducer, action and page, redirect to login page if reset success

